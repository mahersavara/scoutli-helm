apiVersion: v1
kind: Service
metadata:
  name: {{ include "scoutli-app.fullname" . }}
  labels:
    {{- include "scoutli-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scoutli-app.selectorLabels" . | nindent 4 }}
