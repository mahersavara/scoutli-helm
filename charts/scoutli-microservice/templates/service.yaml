apiVersion: v1
kind: Service
metadata:
  name: {{ include "scoutli-microservice.fullname" . }}
  labels:
    {{- include "scoutli-microservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scoutli-microservice.selectorLabels" . | nindent 4 }}
